# Model Weights

This folder is intended to store pretrained Real-ESRGAN model weights.

Download one of the official models and place it here:

- RealESRGAN_x2plus.pth
- RealESRGAN_x4plus.pth

Model files are not included in the repository due to size.
